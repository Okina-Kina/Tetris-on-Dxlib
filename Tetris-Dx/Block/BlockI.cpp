#include "BlockI.h"

//--------------------------------------------------------------------------------------
BlockI::BlockI(int x, int y, int rot)
	:Block(x, y, rot, GetColor(100, 255, 255)) {
	initShape();
}
//--------------------------------------------------------------------------------------
void BlockI::initShape() {
	for (auto&& i = 0; i < s_size; i++)	_shapeData[i][0] = 1;
}
//--------------------------------------------------------------------------------------
